package com.ntsinga.mobile

import android.app.admin.DeviceAdminReceiver

/** Device-admin entry point used when AI-OS is provisioned as a managed device owner. */
class AiosDeviceAdminReceiver : DeviceAdminReceiver()
